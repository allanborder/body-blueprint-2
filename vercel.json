{
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ],
  "buildCommand": "cd fitness-app && npm install && npm run build",
  "outputDirectory": "fitness-app/dist",
  "installCommand": "cd fitness-app && npm install"
}